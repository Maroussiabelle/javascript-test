

/*
Объявите функцию, в которой будут содержаться инструкции запроса числа от пользователя
и вывода квадрата этого числа. Вызовите эту функцию.
 */
function makeExp () {
    /* let userInput = Number(prompt()); */
    let userInput = 5;
    console.log(userInput ** 2);

}

// Вызов функции

makeExp(); // круглые скобки обязательны при вызове


