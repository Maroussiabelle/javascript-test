/*Напишите программу, которая запрашивает ввод строки.
Если эта строка начинается с буквы "ж" в верхнем или нижнем регистре,
программа выводит длину строки, а иначе выводится сообщение "Попробуйте снова". Используйте метод startsWith. */

const input = prompt();
if (input.startsWith("ж") || input.startsWith("Ж")) {
    console.log(input.length);
} else {
    console.log("Попробуйте снова");
}