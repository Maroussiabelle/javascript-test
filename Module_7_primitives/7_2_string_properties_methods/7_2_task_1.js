// Попросите пользователя ввести строку. Если длина строки меньше 5 символов,
// добавьте к строке "!" и выведите результат, иначе добавьте к строке "?"
// и также выведите результат.

const input = prompt();
if (input.length<5){
    console.log(`${input}!`)
} else {
    console.log(`${input}?`)
}